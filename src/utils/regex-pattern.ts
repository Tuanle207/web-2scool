
export const EMAIL_PATTERN = /^\w+([.-]?\w+)*@\w+([.-]?\w+)*(\.\w{2,3})+$/;

export const PASSWORD_PATTERN = /^[A-Za-z]\w{6,19}$/;

// eslint-disable-next-line
export const TENANT_NAME_PATTERN = /^[a-zA-Z\-]+$/;